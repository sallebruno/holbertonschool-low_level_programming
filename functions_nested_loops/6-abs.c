#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 *
 * @n: Integer to compute.
 * Return: Value of integer.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
