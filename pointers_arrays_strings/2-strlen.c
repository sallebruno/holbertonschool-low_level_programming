#include "main.h"
/**
 * _strlen - cuenta
 *
 * @s: almacena
 *
 * Return: retona la cantidad de los caracteres de la cadena
 */
int _strlen(char *s)
{
	int lent;

	for (lent = 0; s[lent] != '\0'; lent++)
	{
	}
	return (lent);
}
